import 'package:flutter/material.dart';

class UltraWideScreen extends StatelessWidget {
  const UltraWideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('UltraWideScreen')),
      body: const Center(child: Text('UltraWideScreen')),
    );
  }
}
