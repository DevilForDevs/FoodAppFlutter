import 'package:flutter/material.dart';

class WebUltraWideScreen extends StatelessWidget {
  const WebUltraWideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WebUltraWideScreen')),
      body: const Center(child: Text('WebUltraWideScreen')),
    );
  }
}
