import 'package:flutter/material.dart';

class WebTabletScreen extends StatelessWidget {
  const WebTabletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WebTabletScreen')),
      body: const Center(child: Text('WebTabletScreen')),
    );
  }
}
