import 'package:flutter/material.dart';

class WebMobileScreen extends StatelessWidget {
  const WebMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('WebMobileScreen')),
      body: const Center(child: Text('WebMobileScreen')),
    );
  }
}
